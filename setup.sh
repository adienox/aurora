#!/usr/bin/env bash

ln -s "$HOME"/aurora/assets/scripts ~/.config

ln -s "$HOME"/aurora/.config/doom ~/.config
ln -s "$HOME"/aurora/.config/nvim ~/.config
ln -s "$HOME"/aurora/.config/rofi ~/.config
ln -s "$HOME"/aurora/.config/tmux ~/.config
